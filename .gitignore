# IDE
.idea/
*.iml

# Local junction/symlink to .agents/skills (recreate per clone, see AGENTS.md)
.claude/skills/

# Setup kit (used to generate AGENTS.md/.agents/.claude — not part of the app)
drop-in-brain-main/
